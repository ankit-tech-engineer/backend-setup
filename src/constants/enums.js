const STATUS = {
  ACTIVE:   'active',
  INACTIVE: 'inactive',
};

const SUPER_ADMIN_KEY = 'super_admin';

const ACTIONS = {
  CREATE: 'create',
  READ:   'read',
  UPDATE: 'update',
  DELETE: 'delete',
  LIST:   'list',
};

module.exports = { STATUS, SUPER_ADMIN_KEY, ACTIONS };
